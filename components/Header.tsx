"use client";
import {useState} from "react"; import Link from "next/link"; import {Menu,X} from "lucide-react"; import Logo from "@/components/Logo"; import {nav,company} from "@/lib/data"; import ButtonLink from "@/components/ButtonLink";
export default function Header(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-slate-200/70 bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3 sm:px-6 lg:px-8"><Logo/><nav className="hidden items-center gap-6 lg:flex">{nav.map(i=><Link key={i.href} href={i.href} className="text-sm font-bold text-slate-700 transition hover:text-electric">{i.label}</Link>)}</nav><div className="hidden items-center gap-3 lg:flex"><a href={`tel:${company.phone}`} className="text-sm font-extrabold text-navy">{company.phone}</a><ButtonLink href="/contact">Schedule a Consultation</ButtonLink></div><button onClick={()=>setOpen(!open)} className="focus-ring rounded-lg p-2 lg:hidden" aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t bg-white px-4 py-4 lg:hidden">{nav.map(i=><Link onClick={()=>setOpen(false)} key={i.href} href={i.href} className="block rounded-lg px-3 py-3 font-bold text-slate-700 hover:bg-slate-50">{i.label}</Link>)}<div className="mt-3"><ButtonLink href="/contact">Schedule a Consultation</ButtonLink></div></div>}</header>}