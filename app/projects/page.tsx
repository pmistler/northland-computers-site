import Link from "next/link"; import {ArrowRight} from "lucide-react"; import {projects} from "@/lib/data"; import {PageHero,SectionHeader,CTA} from "@/components/Sections";
export const metadata={title:"Technology Project Results",description:"Representative Northland Computers projects spanning infrastructure modernization, wireless, device deployment, access control, retail migration, automation, and cloud.",alternates:{canonical:"/projects"}};
export default function Page(){return <main><PageHero eyebrow="Proven results" title="Difficult technology work, planned and delivered." desc="Representative projects demonstrate the scale, breadth, speed, and accountability Northland brings to client environments."/><section className="px-4 py-20 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><SectionHeader title="Experience you can measure." desc="These examples focus on outcomes rather than marketing claims."/><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{projects.map(p=><Link key={p.slug} href={`/projects/${p.slug}`} className="card-hover rounded-3xl border border-slate-200 bg-white p-6"><p className="text-3xl font-black text-electric">{p.metric}</p><h2 className="mt-3 text-xl font-black text-navy">{p.title}</h2><p className="mt-3 leading-7 text-slate-600">{p.summary}</p><div className="mt-4 flex flex-wrap gap-2">{p.tags.map(t=><span key={t} className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600">{t}</span>)}</div><span className="mt-5 inline-flex items-center gap-2 font-extrabold text-electric">View project <ArrowRight className="h-4 w-4"/></span></Link>)}</div></div></section><CTA/></main>}